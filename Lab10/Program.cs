using System;

namespace Lab10
{
    public class Program
    {
        static void Main(string[] args)
        {
            //Ask user how many prime numbers they want to compute
            //They can only enter integer values between 0-10000

            /* Write at least 3 tests for your IsPrime method. Be sure to use AAA and red, green, factor strategies
             *  One for a negative number
             *  another for a valid number
             *  and another for a number larger than 10,000.
             */

            //Write an IsPrime method that takes in a number and returns true if its prime, false if its not prime

            /*You will need some type of loop to output the computed primes
             *  e.g. if a user inputs 20, you should see 20 numbers print out to the console
             */

        }

        public static bool IsPrime(int primeNumber)
        {
            throw new NotImplementedException("You need to make this do something!!");
        }
    }
}
